# Environment Variables Example
# Copy this file to .env and update with your values
# Or set these in your deployment platform's environment variables

# Backend Configuration
# CORS allowed origins (comma-separated)
# For production, add your frontend URLs here
CORS_ORIGINS=https://your-frontend-url.vercel.app,https://your-frontend-url.onrender.com

# Python path (usually not needed, but can be set if required)
PYTHONPATH=/app

# Environment (development, production, staging)
ENVIRONMENT=development

# Logging
LOG_LEVEL=info

# Frontend Configuration (for Vite)
# Set this in your deployment platform's environment variables
# VITE_API_URL=https://your-backend-url.onrender.com

